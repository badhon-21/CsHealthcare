
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}



<!-- Page container -->
<div class="container">
    <div class="profile-content">
        <div class="row">


            <div class="col-md-4">
                <!-- BEGIN PORTLET -->
                <div class="portlet light ">
                    <div class="portlet-title">
                        <div class="caption caption-md">
                            <i class="icon-bar-chart theme-font hide"></i>
                            <span class="caption-subject font-blue-madison bold uppercase">Quick Menu</span>
                            <span class="caption-helper hide">weekly stats...</span>
                        </div>

                        <div class="tools">
                            <a href="javascript:;" class="collapse">
                            </a>

                            @*  <a href="javascript:;" data-load="true" data-url="@Url.Action("patientLocationHistory", "Patient", new {Area = "MsAdmin", id = Model.Id})" c class="reload">
                                </a>*@
                            <a href="javascript:;" class="remove">
                            </a>
                        </div>

                    </div>
                    <div class="portlet-body  portlet-empty">
                        <div class="tiles">
                            <a href="@Url.Action("Create", "Patient", new {area = "PatientInformation"})" class="tile  bg-blue-hoki">

                                <div class="tile-body">
                                    <i class="fa fa-bell-o"></i>
                                </div>
                                <div class="tile-object">
                                    <div class="name">
                                        Test Of Patients
                                    </div>
                                    <div class="number">
                                        
                                    </div>
                                </div>
                            </a>
                            
                            

                        </div>
                    </div>
                </div>
                <!-- END PORTLET -->
            </div>


            <div class="col-md-6">
                <!-- BEGIN PORTLET -->
                <div class="portlet light ">
                    <div class="portlet-title">
                        <div class="caption caption-md">
                            <i class="icon-bar-chart theme-font hide"></i>
                            <span class="caption-subject font-blue-madison bold uppercase">Completed Patient test History</span>
                            <span class="caption-helper hide">weekly stats...</span>
                        </div>

                        <div class="tools">
                            <a href="javascript:;" class="collapse">
                            </a>

                            @* <a href="javascript:;" data-load="true" data-url="@Url.Action("AdminLocationHistory", "Patient", new { Area = "MsAdmin", id = Model.Id })" c class="reload">
                                </a>*@
                            <a href="javascript:;" class="remove">
                            </a>
                        </div>

                    </div>
                    <div class="portlet-body  portlet-empty">

                    </div>
                </div>
                <!-- END PORTLET -->
            </div>

        </div>

    </div>
    <!-- END PROFILE CONTENT -->
</div>
<!-- /page container -->

@section Scripts{
    @*<script>

        $(document).ready(function() {
            Price();
        });
        function Price() {
            $.ajax({
                type: "GET",
                cache: false,
                url: "@(@Url.Action("Price", "AdminDashBoard", new {Area = ""}))",
                data: {  },

            dataType: "html",
            success: function (result) {
               // alert(result);
                var t = JSON.parse(result);


                $('#weekly').text(t.weeklySale);
                $('#monthly').text(t.montlySale);
                $('#all').text(t.totalSale);
                //alert(t);

                //var t = JSON.parse(result);

                //$('#Rate').val(t.price);
                //$('#Rate').val(result);
            },
            error: function(error) {
                //Metronic.unblockUI();
                var msg = 'Fail to load .';
                alert(msg);
            }
        });
        }


    </script>*@
}

